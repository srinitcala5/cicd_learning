name: Deploy Static Web App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: true

    - name: Build And Deploy to Azure
      id: deploy-to-azure
      uses: azure/static-web-apps-deploy@v1 
      with:
        # 🔑 CRITICAL FIX: Using your exact secret name
        azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_WITTY_FLOWER_0BB7C5000 }} 
        
        repo_token: ${{ secrets.GITHUB_TOKEN }} 
        action: "upload"
        app_location: "/" 
        output_location: "."
